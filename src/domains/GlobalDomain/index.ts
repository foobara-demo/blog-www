export * as config from './config'

export const isGlobal = true
export const organizationName = 'GlobalOrganization'
export const domainName = 'GlobalDomain'

export { GetCurrentUser } from '../BlogWww/GetCurrentUser'

export { ComputeExponent } from '.././ComputeExponent'

// TODO: put these on an entities module so that commands can be the only top-level interface.
